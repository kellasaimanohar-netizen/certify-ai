[project]
name = "sample-support-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "langchain>=0.2",
    "langchain-openai>=0.1",
    "httpx>=0.27",
    "pyyaml",
    "requests",
]
